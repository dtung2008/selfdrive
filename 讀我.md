# 自駕模擬器

[English](README.md)

一個模組化的模擬器，用於學習和比較多車道高速公路上的自駕控制策略。本專案實現了從環境模擬、專家資料收集、模型訓練到多智能體評估的完整流程。

## 概覽

本專案提供：

- 具有擬真交通流的**多車道高速公路模擬器**（IDM 跟車模型、MOBIL 換道模型）
- 涵蓋規則式、監督式學習與模型預測控制的四種**駕駛智能體**
- 基於**注意力機制的世界模型**（Transformer），學習預測交通動態
- **訓練流程**，包括行為複製與世界模型學習
- **評估框架**，支援智能體並排比較，指標包括碰撞率、平均速度與回合完成率

## 智能體比較

| 智能體 | 類型 | 說明 |
|---|---|---|
| **Expert** | 規則式 | 加速度感知的縱向控制，具備安全換道決策、緊迫度升級機制，以及透過 `ExpertConfig` 設定的可調參數。用於產生監督式學習的示範資料。 |
| **Behavior Cloning (BC)** | 監督式 | MLP 策略網路，透過交叉熵損失與逆頻率類別加權來模仿專家行為，對稀有動作給予更高權重。 |
| **Planner (true model)** | MPC | 隨機射擊式模型預測控制，使用真實模擬器的 `clone_state`/`restore_state` 進行精確前向模擬。在可設定的預測範圍內評估 9 種操作模板。使用分離的隨機數生成器分別處理候選方案生成與平局打破。 |
| **Planner (learned WM)** | MPC | 相同的 MPC 框架，但以學習到的注意力世界模型取代真實模擬器。完全在 PyTorch 中執行前向模擬以提升速度。 |

## 交通模型

NPC 車輛使用來自交通模擬文獻的擬真跟車與換道模型：

- **Constant Speed** —— 基準車輛，以比例控制維持固定目標速度。
- **IDM (Intelligent Driver Model)** —— 基於物理的跟車模型（Treiber et al., 2000），可設定期望速度 `v0`、時間車距 `T`、最小間距 `s0`、舒適加速度/煞車力 `a`/`b`。產生平滑、無碰撞的縱向行為。
- **MOBIL (Minimizing Overall Braking Induced by Lane changes)** —— 基於激勵的換道模型（Kesting et al., 2007），疊加在 IDM 之上。檢查安全準則（跟隨車減速度 < 閾值）與激勵準則（淨加速度增益 > 禮讓加權成本 + 閾值）。
- **個性化變體** —— 對 IDM/MOBIL 參數加入高斯雜訊，產生激進與保守駕駛者的混合，增加訓練資料分佈的多樣性。

## 架構

### 動作空間

9 個離散動作，排列為 3×3 網格：

```
                  橫向
              LEFT  KEEP  RIGHT
           +------+------+------+
DECELERATE |  0   |  1   |  2   |
縱向 KEEP  |  3   |  4   |  5   |
ACCELERATE |  6   |  7   |  8   |
           +------+------+------+

索引公式: (longitudinal + 1) * 3 + (lateral + 1)
```

### 觀測空間

固定大小的向量，包含自車狀態與 K 個最近鄰居（預設 K=10）：

```
[ego_speed, ego_lane, ego_x, ego_accel,         # 4 個值：自車狀態
 rel_x_1, rel_lane_1, rel_speed_1, rel_accel_1, exists_1,   # 每個鄰居 5 個值
 rel_x_2, rel_lane_2, rel_speed_2, rel_accel_2, exists_2,   # 依距離排序
 ...                                                          # 不足 K 個則補零
 rel_x_K, rel_lane_K, rel_speed_K, rel_accel_K, exists_K]   # 總計: 4 + 5*K = 54
```

### 注意力世界模型

世界模型使用 Transformer 編碼器來預測下一狀態的差值：

1. **逐車嵌入** —— 每輛車的特徵透過獨立的 MLP（自車與鄰居分開）投影至 `embed_dim`
2. **動作注入** —— 自車動作以 one-hot 編碼串接後再進行嵌入
3. **類型嵌入** —— 學習到的標記用於區分自車（位置 0）與 NPC 車輛
4. **Transformer 編碼器** —— 多頭自注意力捕捉車輛間交互作用（例如煞車連鎖反應）。注意力遮罩處理可變的鄰居數量
5. **殘差預測** —— 輸出頭預測狀態*差值*而非絕對值，當連續狀態相似時更容易學習

## 專案結構

```
selfdrive/
├── environment/                     # 模擬引擎
│   ├── road.py                      # 道路幾何：N 車道、車道寬度、邊界檢查
│   ├── vehicle.py                   # 一階運動學動力學（歐拉積分）
│   ├── traffic_behaviors.py         # NPC 行為：Constant Speed、IDM、MOBIL
│   ├── traffic_manager.py           # NPC 生命週期：生成、步進、消除、重生
│   ├── observation.py               # 以自車為中心的固定大小觀測建構器
│   └── simulator.py                 # 類 Gym 的 step/reset 介面與獎勵計算
├── agents/                          # 駕駛智能體
│   ├── base.py                      # 抽象 Agent 介面：act(obs) -> Action
│   ├── expert.py                    # 具 IDM 式邏輯的規則式專家
│   ├── bc_agent.py                  # 行為複製智能體（含安全覆寫）
│   ├── planner_true.py              # 使用真實模擬器前向模擬的 MPC 規劃器
│   └── planner_learned.py           # 使用學習世界模型的 MPC 規劃器
├── models/                          # 神經網路架構
│   ├── policy_net.py                # MLP 策略：obs -> action logits（9 類）
│   └── world_model.py              # Transformer 世界模型：(obs, action) -> next_obs
├── training/                        # 訓練迴圈與資料收集
│   ├── data_collector.py            # 透過智能體—環境互動收集軌跡
│   ├── bc_trainer.py                # 行為複製：交叉熵 + 類別加權
│   ├── world_model_trainer.py       # 世界模型：正規化觀測差值的 MSE
│   └── evaluator.py                 # 多智能體評估與統計指標
├── utils/                           # 共用型別與設定
│   ├── types.py                     # 核心資料類別：Action、VehicleState、Observation、
│   │                                #   Transition、Trajectory
│   └── config.py                    # 所有超參數：道路、車輛、交通、模擬、
│                                    #   觀測、模型、專家設定
├── tests/                           # 103 個涵蓋所有模組的單元測試
│   ├── test_types_config.py         # Action 編碼、VehicleState、Observation、Config
│   ├── test_road_vehicle.py         # 道路幾何、VehicleDynamics 步進函式
│   ├── test_traffic_behaviors.py    # IDM/MOBIL/ConstantSpeed + 輔助函式
│   ├── test_traffic_manager.py      # NPC 生成/消除、間距保證、多車道
│   ├── test_observation.py          # 觀測形狀、排序、填充、截斷
│   ├── test_simulator.py            # Step/reset、碰撞、獎勵、clone/restore
│   ├── test_expert.py               # 在精心設計的觀測上測試專家決策
│   ├── test_models.py               # PolicyNetwork + AttentionWorldModel 形狀/梯度
│   ├── test_training.py             # DataCollector、BCTrainer、WorldModelTrainer
│   └── test_planners_eval.py        # PlannerTrue、PlannerLearned、Evaluator
├── run_comparison.py                # 完整流程：收集資料、訓練所有智能體、比較
├── run_simple_debug.py              # 簡化的 1 車道比較，用於隔離縱向控制問題
├── record_episodes.py               # 將回合重播錄製為 JSON 供瀏覽器視覺化
├── record_debug.py                  # 增強版除錯錄製，包含每步驟的專家比較
├── trace_bc_collision.py            # 逐步 BC 碰撞診斷（BC vs Expert）
├── test_wm_horizon.py              # WM 規劃器跨預測範圍的誤差累積測試
├── verify_wm_rollout.py            # 混合 WM 前向模擬精度 vs 真實模擬器
├── debug_planner_decision.py        # 規劃器在卡住/低速步驟的獎勵分解
├── scan_failures.py                 # 掃描種子範圍以發現智能體失敗回合
├── debug_viewer.html                # 瀏覽器端回合視覺化（載入 JSON 重播）
├── requirements.txt                 # 依賴套件：numpy、torch、pytest
└── .gitignore
```

## 安裝設定

### 前置需求

- Python 3.9+
- pip

### 安裝

```bash
pip install -r requirements.txt
```

依賴套件：
- `numpy >= 1.21` —— 陣列運算與線性代數
- `torch >= 2.0` —— 神經網路與自動微分（可用時自動使用 CUDA）
- `pytest >= 7.0` —— 測試框架

## 執行測試

執行全部 103 個測試：
```bash
python -m pytest tests/ -v
```

執行特定測試檔案：
```bash
python -m pytest tests/test_traffic_behaviors.py -v
python -m pytest tests/test_simulator.py -v
```

執行單一測試：
```bash
python -m pytest tests/test_expert.py::TestExpertAgent::test_accelerates_on_empty_road -v
```

## 執行完整比較

訓練所有智能體並進行並排評估：

```bash
python run_comparison.py
```

### 命令列選項

```bash
python run_comparison.py \
  --num-lanes 2 \              # 高速公路車道數（預設：2）
  --num-npcs 6 \               # NPC 車輛數（預設：6）
  --collect-episodes 50 \      # 專家資料收集回合數（預設：50）
  --bc-epochs 200 \             # 行為複製訓練輪數（預設：200）
  --wm-epochs 20 \             # 世界模型訓練輪數（預設：20）
  --eval-episodes 30           # 每個智能體的評估回合數（預設：20）
```

### 流程步驟

1. **資料收集** —— 專家智能體在模擬器中駕駛，收集（觀測, 動作）軌跡
2. **BC 訓練** —— 透過監督式學習訓練 MLP 策略來模仿專家動作
3. **世界模型訓練** —— 訓練注意力世界模型來預測下一步觀測
4. **評估** —— 對所有智能體執行 N 個回合，收集以下指標：
   - 平均獎勵與標準差
   - 平均速度（m/s）
   - 碰撞率
   - 平均回合長度
   - 每回合換道次數
   - 回合完成率

## 除錯與診斷工具

本專案包含數個用於診斷智能體行為與世界模型準確度的腳本。

### 簡化的 1 車道除錯

移除所有換道來隔離縱向控制。在此設定下所有智能體應達到 0% 碰撞率：

```bash
python run_simple_debug.py --seed 42 --eval-episodes 20
```

### 錄製與視覺化回合

將回合錄製為 JSON，然後在瀏覽器中開啟 `debug_viewer.html` 進行視覺化：

```bash
# 錄製專家智能體重播
python record_episodes.py --num-lanes 2 --output replay_data.json

# 錄製任意智能體的增強除錯資料（包含專家比較）
python record_debug.py --agent bc --seed 42 --output debug_episode.json
python record_debug.py --agent planner_wm --wm-epochs 80 --output debug_episode.json
```

然後在瀏覽器中開啟 `debug_viewer.html` 並載入 JSON 檔案。

### 診斷 BC 碰撞

找到一個 BC 智能體發生碰撞的種子，然後逐步重播比較 BC 與 Expert 的決策：

```bash
python trace_bc_collision.py
```

輸出顯示每步驟的速度、間距，以及 BC 與專家不一致的位置（可能的碰撞原因）。

### 世界模型準確度

測試 WM 預測誤差是否隨規劃範圍增加而累積：

```bash
# 碰撞率 vs 規劃範圍（預期在較長範圍時急遽上升）
python test_wm_horizon.py

# 逐步比較：混合 WM 前向模擬 vs 真實模擬器
python verify_wm_rollout.py
```

### 掃描失敗種子

訓練智能體一次，然後在一系列種子上執行。僅對提前結束的回合（碰撞或其他提前終止）輸出除錯 JSON，跳過完整長度的回合。適用於追蹤罕見的失敗模式，無需逐一手動嘗試種子：

```bash
# 掃描種子 0-100 尋找 planner_wm 失敗
python scan_failures.py --agent planner_wm --seeds 0-100

# 使用特定設定掃描
python scan_failures.py --agent bc --seeds 0-50 --num-lanes 1

# 將失敗 JSON 檔案儲存到目錄
python scan_failures.py --agent planner_wm --seeds 0-200 --wm-epochs 80 --output-dir failures/
```

輸出包含失敗率摘要及與 `debug_viewer.html` 相容的逐次失敗 JSON 檔案。

### 規劃器決策除錯

當 WM 規劃器「卡住」（速度 < 22 m/s 持續 5 步以上）時，輸出其內部獎勵分解：

```bash
python debug_planner_decision.py --seed 42 --debug-steps stuck
python debug_planner_decision.py --seed 42 --debug-steps 50,100,150
```

## 效能比較

各車道設定的平均獎勵（每組 3 個種子，300 步，規劃器使用 h=30, r=50）：

| 設定 | Expert | BC | Planner True | Planner WM |
|-----------|--------|--------|--------------|------------|
| 1L / 3NPC | 200.4 | 198.9 | 203.5 | 198.9 |
| 2L / 6NPC | 117.4 | 215.5 | 239.7 | 246.9 |
| 3L / 9NPC | 281.9 | 204.5 | 244.4 | 231.8 |
| 4L / 12NPC | 204.9 | 189.6 | 242.2 | 237.5 |

關鍵觀察：
- **1L**：所有智能體表現相近（~200）。簡單的跟車問題所有方法都能良好處理。
- **2L**：Expert 在不同種子間波動劇烈（33–271），而 BC 與兩個規劃器則表現穩定。Planner WM 在此略優於 Planner True（247 vs 240）。
- **3L/4L**：Expert 在啟發式規則找到好車道時表現優異（282–287），但在困難種子上可能崩潰（46）。規劃器最為穩定（~240），而 BC 停滯在約 200。
- **BC**：得益於安全覆寫層，在所有設定中零碰撞，但 MLP 無法複製多步換道推理，獎勵上限約為 200。
- **Planner WM vs True**：儘管使用的學習模型僅訓練了 20 輪，兩者表現卻非常接近。

## 關鍵設計決策

### 模組化
每個模組都有清晰的介面與獨立的測試檔案。新增智能體只需：
1. 在 `agents/` 中繼承 `Agent` —— 實作 `act(obs: np.ndarray) -> Action`
2. 將其加入 `run_comparison.py` 中的比較字典
3. 在 `tests/` 中撰寫測試

### 可擴展性
- **更多車道** —— 修改 config 中的 `num_lanes`；所有模組皆支援 N 車道道路
- **連續動作** —— 修改 `Action` 類別與 `VehicleDynamics`；智能體與模型自動適配
- **更豐富的世界模型** —— 在 `world_model.py` 中加入遞迴或圖注意力機制；規劃器介面不變
- **新的交通行為** —— 繼承 `TrafficBehavior` 並加入 `TrafficConfig` 的行為組合

### CUDA 支援

所有神經網路模型（PolicyNetwork、AttentionWorldModel）在 GPU 可用時自動放置於 CUDA。`record_debug.py` 與 `run_comparison.py` 在啟動時偵測裝置並傳遞給模型建立。訓練器（`BCTrainer`、`WorldModelTrainer`）與推論智能體（`BCAgent`、`PlannerLearnedModel`）從模型參數推導裝置，因此張量自動搬移。

### 專家設定

專家智能體的行為完全透過 `utils/config.py` 中的 `ExpertConfig` 參數化。主要參數包括：

- **間距閾值**：`normal_min_gap_ahead/behind`、`emergency_gap_ahead/behind`、`critical_gap`
- **速度控制**：`desired_speed`、`speed_deadband_low/high`、`max_decel`
- **換道**：`lane_change_gap`、`lane_change_cooldown`、`min_improvement`、`closing_speed_threshold`
- **緊迫度升級**：`urgency_escalation_steps`、`max_urgency_escalation` —— 僅放寬前方間距（絕不放寬後方間距，因其為安全限制）
- **前瞻**：`proactive_lane_change_time`、`two_step_lookahead_factor`、`gap_projection_time`
- **連鎖偵測**：`cascade_accel_threshold`、`cascade_max_gap`

### 獎勵結構

模擬器中的獎勵函數結合多個項目：
- **碰撞懲罰** —— 碰撞時給予大幅負獎勵（預設：-100）
- **速度獎勵** —— 接近速限時的線性獎勵
- **換道懲罰** —— 小幅懲罰以抑制不必要的換道
- **急煞懲罰** —— 懲罰突然減速以維持乘坐舒適度
